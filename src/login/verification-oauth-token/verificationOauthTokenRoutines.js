import { createRoutine } from 'redux-saga-routines';

export const verificationOauthTokenLoginRoutine = createRoutine(
  'VERIFICATION_OAUTH_TOKEN_LOGIN',
);

export default verificationOauthTokenLoginRoutine;
